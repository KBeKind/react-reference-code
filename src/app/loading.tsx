import React from "react";

// this is the default loading page
// can add animations etc
// if it is a server component that is fetching data then this loading page will display

const loadingPage = () => {
  return <div>loading</div>;
};

export default loadingPage;
