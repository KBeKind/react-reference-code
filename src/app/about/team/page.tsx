import React from "react";
// you can add metadata for title or SEO directly on a page as well
export const metadata = {
  title: "About Team KBeKind",
};

const TeamPage = () => {
  return <div>Dis is TeamPage</div>;
};

export default TeamPage;
